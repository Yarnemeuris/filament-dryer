# Firmware

Here you can find the Klipper config. The firmware for the Pico itself can be obtained via Klipper itself. It should have the default settings, but communication is via CANbus with RX on pin 4 and TX on pin 5.